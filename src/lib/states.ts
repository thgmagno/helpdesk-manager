export interface CustomerFormState {
  errors: {
    name?: string[]
    phone?: string[]
    email?: string[]
    address?: string[]
    _form?: string
  }
}

export interface TicketFormState {
  errors: {
    name?: string[]
    phone?: string[]
    email?: string[]
    address?: string[]
    _form?: string
  }
}
